const range = document.querySelector('#range');
const startBtn = document.querySelector('#checkbox');
const q = document.querySelector('#range');

const ref = { range, startBtn };
export default ref;
